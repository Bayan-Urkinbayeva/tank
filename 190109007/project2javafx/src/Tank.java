//package sample;

import javafx.scene.image.ImageView;

public class Tank extends MyPlayer{
    public Tank(String position,ImageView imageView){

        super(position, imageView);
    }
}
